% this example was copied from the original Dedalus paper



seq(B)@next <- seq(A), successor(A, B), event(_);
seq(A)@next <- seq(A), not event(_);

event(one)@100;
event(two)@200;

seq(500);

test_passed <-
  seq(501),
  seq(502);
